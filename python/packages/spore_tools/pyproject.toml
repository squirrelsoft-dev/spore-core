[project]
name = "spore-tools"
version = "0.0.0"
description = "Standard Tool implementations for spore-core"
requires-python = ">=3.12"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/spore_tools"]
