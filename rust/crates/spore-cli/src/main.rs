//! spore-cli: thin command-line wrapper that constructs a `Harness`
//! (see spec: Deployment Surfaces) and drives `run()` / `resume()`.
//!
//! This binary does not implement any harness components — those live
//! in `spore-core` (issues #1–#13).

fn main() {}
