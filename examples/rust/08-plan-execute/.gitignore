/target/
# Agent run-artifacts: the workspace is scratch output. Keep the seed and the
# canonical committed report; ignore everything else the plan-execute run emits.
workspace/*
!workspace/.gitkeep
!workspace/Async_Runtime_Comparison_Report.md
