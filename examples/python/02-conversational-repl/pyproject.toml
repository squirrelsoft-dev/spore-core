[project]
name = "spore-example-02-conversational-repl"
version = "0.0.0"
description = "spore-core example 02 — conversational REPL (Python)"
requires-python = ">=3.12"
dependencies = ["spore-core"]

[tool.uv.sources]
spore-core = { path = "../../../python/packages/spore_core", editable = true }
