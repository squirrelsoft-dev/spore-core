/**
 * Public re-exports for the canonical {@link CacheProvider} (spore-core
 * issue #25). Re-exported under the `cacheProvider` namespace at the package
 * root for symmetry with `promptChunkRegistry`, `memory`, etc.
 */

export * from "./types.js";
