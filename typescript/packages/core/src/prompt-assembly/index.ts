/**
 * Public re-exports for the prompt assembly engine (spore-core issue #79).
 *
 * Re-exported under the `promptAssembly` namespace at the package root,
 * symmetric with `promptChunkRegistry`, `context`, etc. Extends (does not
 * replace) the #24 `prompt-chunk-registry` — see {@link "./types.js"}.
 */

export * from "./types.js";
