/**
 * Public re-exports for the {@link Verifier} component (spore-core issue #44).
 * Re-exported under the `verifier` namespace at the package root for symmetry
 * with `cacheProvider`, `promptChunkRegistry`, `memory`, etc.
 */

export * from "./types.js";
